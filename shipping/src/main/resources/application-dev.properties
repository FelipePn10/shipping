# ============================================
# ? PERFIL DE DESENVOLVIMENTO
# ============================================

# Database (valores vêm das variáveis de ambiente do Docker)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/redirex_main}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:redirex_admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Xb2@pQ!v7yRz39Lk}

# Redis
spring.data.redis.host=${SPRING_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_REDIS_PASSWORD:Q!sT4@hG9rX2#LpE}

# JPA/Hibernate (apenas para DEV)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging DEV
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.redirex.shipping=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# HikariCP Optimizations
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=20000

# Actuator (para health checks)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always